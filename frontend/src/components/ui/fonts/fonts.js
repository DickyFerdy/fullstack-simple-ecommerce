import { Inter, Lato, Kanit } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin']
});

export const lato = Lato({
  subsets: ['latin'],
  style: 'normal',
  weight: '700'
});

export const kanit = Kanit({
  subsets: ['latin'],
  style: 'normal',
  weight: '500'
});